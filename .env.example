VITE_API_URL=http://localhost:5000/api

# Optional: public .splinecode URL for the hero's 3D travel globe.
# Leave unset to use the built-in CSS/Motion fallback globe.
VITE_SPLINE_SCENE_URL=

# Demo/recording mode: skip the API and serve bundled demo data.
VITE_DEMO_MODE=false
